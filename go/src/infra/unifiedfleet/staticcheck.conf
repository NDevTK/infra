checks = [
  "all",
  # UFS has too many calls to deprecated functions. Defer for now (SA1019).
  "-SA1019",
  # Defer adding package doc comments (ST1000).
  "-ST1000",
  # Defer renaming values and functions (ST1003).
  "-ST1003",
  # _kind fields for datastore values are legitimately unused in UFS.
  "-U1000",
]
