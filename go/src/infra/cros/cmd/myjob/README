# ChromeOS Myjobs

This package implements the `cros myjob` CLI, a successor to the old `cros
tryjob` CLI. It enables users to run ad-hoc ChromeOS builders with common
custom configurations.

This tool is actively being renamed as `cros try`. Please see the ../try package.
