package main

import (
	"testing"

	. "github.com/smartystreets/goconvey/convey"
)

func TestDatastore(t *testing.T) {
	Convey("Datastore Tests", t, func() {
		Convey("No tests right now. Should use gae to test this; their library is very hard to test.", func() {})
	})
}
