name: infra/chromium-go-url-redirector
extends: ../base.yaml

inputsdir: ../../../go/src/infra/third_party/govanityurls

build:
  - go_gae_bundle: ${inputsdir}/app.yaml
    go_gae_bundle_as_module: True
    dest: ${contextdir}/govanityurls

