name: infra/test-resources
extends: ../base.yaml

contextdir: ../../../go/src/infra/appengine/test-resources

build:
  # This builds the frontend code, updating ${inputsdir}/frontend/build.
  - run: ['make', 'build']
    cwd: ${contextdir}
    outputs:
      - ${contextdir}/static

  - go_gae_bundle: ${contextdir}/app.yaml
    dest: ${contextdir}/appengine
