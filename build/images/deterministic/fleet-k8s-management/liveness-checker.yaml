---
name: fleet-k8s-management/liveness-checker
extends: ../../base.yaml

dockerfile: ../../../../go/src/infra/cros/cmd/k8s-management/liveness-checker/docker/Dockerfile
deterministic: true

build:
  - go_binary: infra/cros/cmd/k8s-management/liveness-checker
    # ContextDir defaults to the directory with Dockerfile
    dest: ${contextdir}/liveness-checker
