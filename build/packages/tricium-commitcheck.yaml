package: infra/tricium/legacy_functions/commitcheck/${platform}
description: Check for the presence of both TEST= and BUG= entries
update_latest_ref: true
platforms:
  - linux-amd64
go_packages:
  - infra/tricium/functions/commitcheck
root: ../../go/bin
data:
  - file: commitcheck${exe_suffix}
  - version_file: .versions/commitcheck${exe_suffix}.cipd_version
